stage: stage_two_ocr_auto
description: Normal mixed-input entry point. The pipeline parses structured data and text layers first, then runs OCR only for pages with no usable text or a direct result that needs fallback; GPU/CPU is selected automatically.
