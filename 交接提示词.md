# 交接提示词（直接粘贴给执行 Agent）

---

你是一名开发 Agent，接手一个"本地发票台账 Skill"的修复与扩展工程。**唯一任务书是 `E:\maple192600-li-invoice-ledger-1.0.1\实施计划.md`**——所有任务、测试方法、验收标准以它为准，严格逐项执行，不要临场重构、不要扩大范围。

## 开工前必读（按顺序，读完再动手）

1. `E:\maple192600-li-invoice-ledger-1.0.1\实施计划.md` —— 任务书（含环境、资产、铁律、风险）
2. `E:\maple192600-li-invoice-ledger-1.0.1\数电票扩展总体方案.md` —— 架构决策与阶段划分
3. `E:\maple192600-li-invoice-ledger-1.0.1\REVIEW.md` —— 12 项问题的证据
4. `E:\maple192600-li-invoice-ledger-1.0.1\P1-修复方案.md` 与 `电子凭证工具包分析.md`（附录有官方字段中英对照表）—— P1 细节与字段语义标准

## 环境事实（已验证，直接用）

- 单一事实源（全部改动在此）：`E:\maple192600-li-invoice-ledger-1.0.1`
- Python 解释器（依赖已装齐，无需新建环境）：`C:\Users\Administrator\.workbuddy\skills\invoice-ledger\.venv\Scripts\python.exe`，用它直接跑源仓库脚本
- 回归测试集：源仓库 `数电发票\` 下 36 张真实 PDF；独立 XML 样本在 `C:\Users\Administrator\Desktop\财务相关资料\高德打车电子发票\`（只读引用，不要移动）
- 回归基线：源仓库 `回归基线\v0\`（未修改版本 × 36 票的结果，作为全部对比基准）
- 官方字段配置（8 个，已入库）：源仓库 `config\xbrl\`

## 铁律（违反即返工）

1. 零新增第三方依赖（只用现有 venv 包 + Python 标准库）
2. 不做验签、云查验、OFD 解析、Java/jar、会计入账字段、银行/国库凭证
3. 无真实样本验证的能力，留接口并注明"未验证"，不宣称支持
4. 只改源仓库；`C:\Users\Administrator\.workbuddy\skills\invoice-ledger` 仅 Phase 3 同步
5. 税务口径只呈现票面/官方数据与来源；抵扣建议一律写"与主管税务机关确认"
6. 每个任务完成后，把测试命令与输出摘要追加到源仓库 `实施记录.md`

## 执行协议

1. 按实施计划的 Phase 1 → 2 → 3 顺序执行；每个任务先读"改动"再写代码，写完立即跑该任务的"测试"，对照"验收"逐条打勾
2. 每个 Phase 结束时必须通过对应验收门（G1/G2/G3），不通过就回退该 Phase 改动、修复、重验，不得带病进入下一 Phase
3. 基线对比用 openpyxl 逐行逐列 diff 两个 xlsx，差异必须落在计划列出的"允许差异清单"内
4. 计划与代码现状冲突、或出现计划未覆盖的情况：停下来，把冲突点写进 `实施记录.md` 并向用户请示，不要自行决断
5. 全部完成后交付：变更文件清单、三次回归的差异总表、golden test 输出、doctor 输出、`实施记录.md`

## 完成定义

G1 + G2 + G3 全部通过：两张铁路票（112.00/102.75/9%/9.25/售；38.00/35.85/6%/2.15/退）、两张航空票（吉祥合计 1100.00、东航合计 1000.00）、高德 XML（13.10/0.39/13.49，含 -0.90 折扣行）与票面/官方数据逐项一致；36 票回归无计划外差异；安装副本同步后 doctor 全过。
