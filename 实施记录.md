# 实施记录

- **任务书**：`实施计划.md`（Phase 1 → 2 → 3，严格逐项执行）
- **执行方式**：Claude Code 一口气做完模式（遇计划未覆盖/冲突才停下请示）
- **回归基线 v0**：`回归基线/v0/`（未修改版本 × 36 票：37 单元、112 行、68 通过、44 待复核）
- **测试集**：`数电发票/` 36 张真实 PDF；高德 XML 在桌面（只读引用）
- **venv**：`C:/Users/Administrator/.workbuddy/skills/invoice-ledger/.venv/Scripts/python.exe`
- **运行命令模板**（源仓库根目录）：
  ```
  PY="C:/Users/Administrator/.workbuddy/skills/invoice-ledger/.venv/Scripts/python.exe"
  "$PY" scripts/fp_ledger.py --input-dir "数电发票" --draft-ledger <台账.xlsx> --config config/runtime.yaml --output-dir <输出目录> --json-output summary
  ```

---

## 准备

- [x] 建 `.gitignore`：排除 `数电发票/`、`回归基线/`、`P020260515544445443201.zip`、`output*/`、`units/`、`.venv/`、`.ocr_cache/`、`.workbuddy/`、`电子凭证…工具包/`、`~$*.xlsx`（用户决策：推公开仓库 + 排除真实数据）
- [x] 建 `实施记录.md`（本文件）

---

## Phase 1 清旧账（代码 + G1 验收 PASS）

### 改动
- **T1.1 P2 红蓝标志按金额符号派生**：`contracts.py` LedgerRow 加 `is_positive_invoice`；`ledger_rows._base_values` 按 `total_with_tax<0` 派生"否"/"是"；`current.yaml` 两 sheet 删 `static: 是`
- **T1.2 P4 置信度只降不升**：`record_validator.py` 通过分支删 `max(confidence, 0.9)`，保留识别置信度
- **T1.3 P5 弱身份去重降级**：`template_writer._append_rows` 无发票号码且非数电的行命中重复时仍写入，context_remark + 识别提示页追加"疑似重复（弱身份票）"；新增返回 `weak_duplicate_rows`
- **T1.4 P7 明细加开票日期列**：模板 detail 第 7 列插入"开票日期"；`current.yaml` detail 加 `invoice_date(required:false)`；template_version→2026-07-23
- **T1.5 P8 死代码清理**：删 `config/draft_ledger_mapping.yaml`、`output/ledger_writer.py`、`feedback/`、`schema/schema_bootstrapper.py`（删前 grep 确认零引用）；INDEX.md 清理 2 条
- **T1.6 P9 处理时间到分钟**：`ledger_rows._processed_date` 不再截断；`template_writer` 新增 `_datetime_value`，processed_at 走 datetime 并设 `YYYY-MM-DD HH:MM`，invoice_date/correction_time 保持日期
- **T1.7 P10 移除山东特判**：`standard-invoice.yaml` 删 title_rules 首条
- **T1.8 P11 版本号统一**：pyproject.toml + SKILL.md → 1.0.1
- **T1.9 P3a OFD 中文提示**：cli._validate_input_paths 对 `.ofd` 报中文指引

### 测试命令
```
PY="C:/Users/Administrator/.workbuddy/skills/invoice-ledger/.venv/Scripts/python.exe"
"$PY" scripts/fp_doctor.py                      # 全 passed（唯一 warning: project_venv，预期）
cp templates/invoice-information-collection.xlsx output/g1/ledger.xlsx
"$PY" scripts/fp_ledger.py --input-dir 数电发票 --draft-ledger output/g1/ledger.xlsx --config config/runtime.yaml --output-dir output/g1 --json-output summary
"$PY" output/_diff.py 回归基线/v0/ledger.xlsx output/g1/ledger.xlsx 发票信息采集
```

### G1 验收结果（PASS）
- 规模与 v0 一致：37 单元 / 112 行 / 68 通过 / 44 待复核 / 0 未建模 / 0 失败
- detail diff 差异（全部在允许清单）：
  - 置信度 67 行（v0=0.9 → new=0.77x，P4）
  - 是否正数发票 2 行（是→否，P2 红冲票）
  - 新增列"开票日期"（T1.4）
- summary diff：仅 是否正数发票 2 行（P2）
- 处理时间：datetime + `YYYY-MM-DD HH:MM`（P9 ✓）
- 开票日期：112/112 有值 + `yyyy-mm-dd` 纯日期（T1.4/T1.6 ✓）
- 弱身份策略：本批无命中，零意外差异
- **G1 通过，进入 Phase 2**

## Phase 2 数电票结构化通道（代码 + G2 验收 PASS）

### 新增/改动
- **T2.2 A1 内嵌 XBRL**：新增 `input_profile/embedded_xbrl.py`——`find_embedded_xbrl`（按内容命名空间识别，不依赖附件名；实测退票票附件 name="0"）、`detect_config_id`（rai/atr/einv/efi/inv_tlf）、`parse_xbrl`（按元素 localname 匹配，与 taxonomy 版本解耦）、`to_invoice_record`（rai 完整映射，其他留接口回退 B 轨）
- **T2.3 CLI 集成**：`pipeline/unit_processor.py` 加 A1（PDF 内嵌优先，命中跳过 OCR/文本层）+ A2（XML）短路 + `_direct_unit_result` 通用组装；`cli.py` `SUPPORTED_INPUT_SUFFIXES` 加 `.xml`；`contracts.py` 加 `FileType.XML`；`file_profile.py` XML 分支；`invoice_units.py` unit_type='xml'
- **T2.4 A2 总局 XML**：新增 `input_profile/einvoice_xml.py`——解析 EInvoice 格式（EIid/IssuItemInformation 明细行含负数折扣/含税语义）
- **T2.5 航空 P1a+P6**：重写 `scheme_extractors/air_ticket.py`——数电变体直读票面（20位号、票价+燃油/票面税额/民航基金单列不计税）；`schemas/air-ticket-itinerary.yaml` 拆 digital/legacy 变体；`config/deductible_vat_rules.yaml` legacy 规则加 `variant_ids`；`deductible_vat.py` `_matches_rule` 支持 variant_ids；`field_candidates.py` scheme_extractor 已设 invoice_no 时跳过通用提取（避免 20位/13位冲突）
- **T2.6 号码归列**：`parsing/invoice_identity.py` `has_standard_digital_invoice_number` 与 schema 解耦（任意 20 位号入数电列）；去重沿用 invoice_line_key + first_row_by_invoice_number（已含 digital_invoice_no 列）
- **data_source 标志**：`contracts.InvoiceQuality` 加 `data_source`（structured/recognition）；`record_validator` 对 structured 跳过版面 evidence 检查（保留金额勾稽），解决高德 XML 因 require_field_evidence 误判复核

### G2 验收结果（PASS）
- 铁路乘车：102.75/9%/9.25/112.00/售（运城北至太原南）✓
- 铁路退票：35.85/6%/2.15/38.00/退（备注"退票费进项抵扣请与主管税务机关确认"）✓
- 吉祥航空：963.30/9%/86.70/1050.00 + 民航基金 50.00，合计 1100.00 ✓
- 东航：825.69/9%/74.31/900.00 + 基金 100.00，合计 1000.00 ✓
- 高德 XML：13.10/0.39/13.49，2 行（14.00/3%/0.42/14.42 与 -0.90/3%/-0.03/-0.93）✓
- 20 位号码全部入"数电发票号码"列 ✓
- 36 票回归 vs G1：配对行零差异，仅铁路 2 + 航空 4 = 6 行 key 变化（字段变好，ready +5）
- 双轨去重：高德第二次跑 added=0、skipped=3 ✓
- **G2 通过，进入 Phase 3**

## Phase 3 验证与同步（T3.1/T3.2 完成）

### T3.1 golden test（PASS）
- 新增 `scripts/fp_golden_test.py`，对 5 组样本断言：铁路乘车/退票、吉祥/东航数电行程单、高德 XML
- 运行：`python scripts/fp_golden_test.py` → **5/5 PASS，退出码 0**

### T3.2 全量回归 v0 vs G2（PASS）
- detail 总差异全部在允许清单：
  - 新增列"开票日期"（T1.4）
  - 置信度 67 行（P4）、是否正数发票 2 行（P2）
  - 仅 v0=6 / 仅 new=6（铁路 2 + 航空 4 = 6 行 Phase2 字段变好）
  - 配对 106 行零计划外差异
- summary：仅是否正数发票 2 行（P2）+ 铁路航空 4 张 key 变
- **零计划外差异，T3.2 通过**

### T3.3 同步安装副本（PASS）
- robocopy /MIR 源仓库 → `.workbuddy/skills/invoice-ledger`（排除 `.venv/output/回归基线/数电发票/工具包/.workbuddy/.git/.claude` 等）
- dry-run 确认仅删除死代码（ledger_writer/feedback/schema_bootstrapper/draft_ledger_mapping）
- 副本 doctor 全 passed（`project_venv` 匹配，无 warning）、副本跑高德 XML 13.10/0.39/13.49 ready 2 行 ✓

### GitHub 同步（完成）
- 远程 `maple192600-LI/invoice-ledger-skill`（PUBLIC，已有 LICENSE/assets/.github 与历史）
- 策略：`merge origin/main -X ours --allow-unrelated-histories` 保留远程历史与基础设施，同名文件取本地最新
- push fast-forward（`1dd8630..ee93baa`），**未 force**
- `.gitignore` 排除真实数据（数电发票/回归基线/104MB zip/output/.venv/.workbuddy/工具包/settings.local.json）

### ⚠️ 关键事件：merge 误覆盖与恢复
- `merge origin/main` 时，`template_writer`/`ledger_rows` 的 **P5(弱身份去重)/P9(处理时间到分钟)** 改动被远程旧版覆盖、死代码被当作远程新增带回（`-X ours` 在 unrelated histories 下未按预期保留本地同名文件）
- 恢复：重新应用 P5/P9 共 9 处改动 + 删除被带回的死代码（4 个文件）
- 验证：golden test 5/5、import OK、4 个恢复标志（`_datetime_value`/`weak_identity`/`return processed_at`/`is_positive` 派生）全在
- 提交 `ee93baa` 修正，HEAD tree 完整正确、无死代码、无隐私数据
- **教训**：跨 unrelated histories 合并时，`-X ours` 不可靠；后续跨仓同步应优先 clone→覆盖→push，或接受单历史
