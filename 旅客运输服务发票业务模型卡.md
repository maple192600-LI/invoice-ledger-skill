# 旅客运输服务发票

- 权威依据：税务发票票面正式标签，以及本轮用户确认的台账业务口径
- 工作结果：识别旅客运输服务发票并写入通用发票台账，旅客行程信息不作为通过条件且不写入 Excel
- 建模样本：1 份受控私有 PDF，为高德打车电子发票
- 建模日期：2026-07-27

## 1. 工作结果范围

| 分类 | 当前需要的内容 | 输出位置或用途 | 边界 |
| --- | --- | --- | --- |
| 直接结果 | 发票号码、日期、购销双方、金额、税额、价税合计、通用明细 | 发票信息采集、发票基础信息 | 按票面明确值写入 |
| 备注结果 | 无旅客行程专属备注 | 无 | 不写出行人、证件号、行程地点等信息 |
| 控制字段 | 出行人、有效身份证件号、出行日期、出发地、到达地、等级、交通工具类型 | 内部识别结果 | 有明确值时保留，缺失时不影响通过 |
| 当前不采集 | 票面未填写的旅客行程信息 | 无 | 不从其他资料补写 |

## 2. 单据身份和状态

| 控制目的 | 国家标准元素或官方标签 | 票面或结构化表现 | 当前要求 | 不满足时 |
| --- | --- | --- | --- | --- |
| 单据种类 | 旅客运输服务 | 票面特定业务类型 | 明确显示该类型 | 保持其他已建模类型或复核 |
| 通用金额 | 金额、税额、价税合计 | 汇总区和明细区 | 通过现有金额校验 | 金额关系异常时复核 |
| 旅客行程 | 出行人等七项票面标签 | 明细区可有标签但无值 | 七项均为可选 | 缺失不触发复核 |

## 3. 工作字段与国家标准对应

| 工作字段 | 国家标准元素 | 官方中文标签 | 含义和类型 | 票面位置或结构层级 | 输出位置 | 缺失或冲突处理 |
| --- | --- | --- | --- | --- | --- | --- |
| 出行人 | 未以结构化样本核对 | 出行人 | 文本 | 旅客运输明细区 | 控制字段 | 缺失留空 |
| 证件号 | 未以结构化样本核对 | 有效身份证件号 | 文本 | 旅客运输明细区 | 控制字段 | 缺失留空 |
| 出行日期 | 未以结构化样本核对 | 出行日期 | 日期 | 旅客运输明细区 | 控制字段 | 缺失留空 |
| 出发地 | 未以结构化样本核对 | 出发地 | 文本 | 旅客运输明细区 | 控制字段 | 缺失留空 |
| 到达地 | 未以结构化样本核对 | 到达地 | 文本 | 旅客运输明细区 | 控制字段 | 缺失留空 |
| 等级 | 未以结构化样本核对 | 等级 | 文本 | 旅客运输明细区 | 控制字段 | 缺失留空 |
| 交通工具类型 | 未以结构化样本核对 | 交通工具类型 | 文本 | 旅客运输明细区 | 控制字段 | 缺失留空 |

## 4. 明细和备注

- 明细单位：票面列示的旅客运输服务项目。
- 直接输出列：沿用通用发票台账列。
- 派生字段及公式：无。
- 备注格式：不生成旅客行程专属备注。
- 空值规则：七项旅客行程字段缺失时留空，不补写、不触发复核。
- 多条或多页规则：沿用通用发票明细规则。

## 5. 不同信息来源怎样对齐

| 工作字段 | XBRL/XML/JSON | PDF 原生文本 | OCR | 统一后的业务值 |
| --- | --- | --- | --- | --- |
| 七项旅客行程字段 | 当前未以结构化样本验证 | 读取票面正式标签和值 | 对应文字 | 可选内部值，不写 Excel |

## 6. 校验关系

| 校验目的 | 涉及字段 | 关系或口径 | 失败处理 |
| --- | --- | --- | --- |
| 类型确认 | 旅客运输服务标签 | 票面证据应指向旅客运输服务 | 证据不足时复核 |
| 金额正确性 | 金额、税额、价税合计和明细金额 | 沿用现有金额校验 | 关系异常时复核 |
| 可选字段 | 七项旅客行程字段 | 缺失不影响通过 | 留空 |
| 输出约束 | 七项旅客行程字段 | 不得进入 Excel 备注或独立列 | 保留内部值，停止写入 |

## 7. 证据边界

- 权威已确认：票面正式标签及其业务含义。
- 真实样本已确认：1 份高德打车电子发票的七项标签存在但值为空，通用金额字段完整。
- 当前未覆盖：七项旅客行程字段写入 Excel 的启用方式。
- 结构化标准映射的使用边界：未证明结构化输入已支持这些字段。
