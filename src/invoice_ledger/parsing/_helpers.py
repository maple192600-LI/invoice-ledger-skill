"""字段抽取的基础工具：候选构造、文本/数值规整、几何/版式、配置读取。"""

from __future__ import annotations

# ruff: noqa: F401

from ._helper_primitives import (
    TAX_ID_RE,
    DATE_RE,
    MONEY_RE,
    LONG_NUMBER_RE,
    TAX_RATE_RE,
    DEFAULT_UNIT_TOKENS,
    DEFAULT_TEXTUAL_SPEC_TOKENS,
    NON_PARTY_FIELD_NAMES,
    TAX_ID_EXCLUDED_CONTEXT_TERMS,
    IDENTITY_NUMBER_CONTEXT_TERMS,
    _compact_number,
    _clean_money,
    _money_matches,
    _compact_text,
    _safe_decimal,
    _has_unclosed_parenthesis,
    _bbox,
    _x0,
    _center_x,
    _y0,
    _center_y,
    _split_two_numbers,
    _decimal_scale,
    _last_number,
    _last_money,
    _last_rate,
    _money_values,
    _has_chinese,
    _is_number_text,
    _is_money_text,
    _looks_like_spec,
    _is_total_marker,
    _join_item_parts,
    _unit_from_parts,
    _split_embedded_spec,
)
from ._helper_fields import (
    _candidate,
    _append_unique_risk,
    _add,
    _role_near_line,
    _add_ocr_confidence_risks,
    _lines,
    _joined,
    _date_candidates,
    _tax_ids,
    _is_tax_id_value,
    _line_confidence,
    _configured_set,
    _schema_section,
    _nested_section,
    _cfg_float,
    _cfg_int,
    _cfg_terms,
    _required_float,
    _required_int,
    _required_terms,
    _first_regex,
    _clean_label_value,
    _value_after_label,
    _number_after_label,
    _money_after_labels,
    _schema_output_default,
    _is_secondary_invoice_identity_context,
    _is_secondary_identity_line,
    _is_direct_secondary_identity_line,
    _is_identity_number_line,
    _is_identity_number_context,
)
from ._helper_layout import (
    _layout_section,
    _unit_in_box,
    _units_in_box,
    _first_regex_in_units,
    _combined_text,
    _normalize_spaced_date,
    _has_bbox,
    _units_with_geometry,
    _page_width,
    _unit_in_y_band,
    _money_values_in_units,
    _ocr_column_for_x,
    _ocr_table_item,
    _normalize_ocr_item,
    _same_row,
    _nearest_money_after_name,
    _money_hits,
    _text_in_layout,
    _units_in_layout,
)
